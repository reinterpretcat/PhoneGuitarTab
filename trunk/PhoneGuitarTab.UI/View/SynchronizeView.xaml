<phone:PhoneApplicationPage
    x:Class="PhoneGuitarTab.UI.View.SynchronizeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
    xmlns:commands="clr-namespace:PhoneCore.Framework.Views.Commands;assembly=PhoneGuitarTab.Core"
    DataContext="{Binding Synchronize, Source={StaticResource Locator}}"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel Grid.Row="0" Margin="12,17,0,28">
            <TextBlock Text="SYNCHRONIZE" Style="{StaticResource PhoneTextNormalStyle}"/>
            <TextBlock Text="skydrive" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}"/>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
            <StackPanel Orientation="Vertical">
                <Button HorizontalAlignment="Stretch" 
                    VerticalAlignment="Top" 
                    IsEnabled="{Binding IsSyncNotRunning}"
                    Height="80"
                    Content="Start!">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <commands:ExecuteCommandAction Command="{Binding LaunchSync, Mode=OneWay}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>

                <ProgressBar
                     Minimum="0"
                     Value="{Binding Progress}"
                     Maximum="100"
                     IsIndeterminate="False" />

                <TextBlock Margin="48,0"
                                   FontFamily="{StaticResource PhoneFontFamilySemiBold}"
                                   Style="{StaticResource PhoneTextNormalStyle}">
                            <Run Text="Uploaded:      " />
                            <Run Text="{Binding UploadedTabs, Mode=OneWay}" />
                </TextBlock>
                <TextBlock Margin="48,0"
                                   FontFamily="{StaticResource PhoneFontFamilySemiBold}"
                                   Style="{StaticResource PhoneTextNormalStyle}">
                            <Run Text="Downloaded: " />
                            <Run Text="{Binding DownloadedTabs, Mode=OneWay}" />
                </TextBlock>
               
            </StackPanel>
            
        </Grid>
    </Grid>

</phone:PhoneApplicationPage>